# Tweet2Telegram: 实时推特监控与 Telegram 转发机器人 🚀

**Tweet2Telegram** 是一个强大的 Python 脚本，它可以实时监控特定的 Twitter (X) 用户的最新推文，并全自动将它们转发到指定的 Telegram 群组或频道中。完美解决错过重要人物推文的烦恼！

---

## 核心功能

- **实时监控**：瞬间侦测并抓取指定推特用户的新推文。
- **Telegram 完美整合**：直接将推文无缝推送到您的 Telegram 群组或频道。
- **高度可定制化**：通过修改 `config.json` 文件即可轻松添加或移除需要监控的博主。
- **支持多媒体附件**：自动捕获推文中的图片，并以原生“图片+图注”的形式发送至 Telegram。
- **轻量且高效**：基于纯 Python 打造，安装简单，运行稳定。

---

## 为什么选择 Tweet2Telegram？

- **永远在第一线**：再也不用担心错过您关注列表里大佬的重要信息。
- **自动化提醒**：节省您人工刷新推特的时间流，让消息主动找你。
- **灵活配置**：随心所欲选择您想监控的目标，以及接收消息的归宿。
- **完全开源**：免费使用，并且您可以随意修改、扩展代码。

---

## 前置准备工作

在开始部署之前，请确保您准备好了以下内容：

- **Python 环境**：本脚本需要 Python 3.8 或以上版本。
- **Telegram 机器人**：去 Telegram 里找 [BotFather](https://core.telegram.org/bots#botfather) 创建一个新的机器人，并拿到它的 Token。
- **Telegram 聊天 ID (Chat ID)**：获取您想接收消息的那个群组或频道的 Chat ID（以 `-100` 开头）。

---

## 安装与部署

1. **克隆项目代码**：
   ```bash
   git clone https://github.com/ChowKwanhwa/real-time-twitter-alert.git
   cd real-time-twitter-alert
   ```

2. **安装依赖包**：
   ```bash
   pip install -r requirements.txt
   ```

3. **配置环境变量**：
   - 将项目目录下的 `.env.example` 文件重命名为 `.env`。
   - 填入您的各项专属配置信息：
     ```plaintext
     BOT_TOKEN=您的_Telegram_机器人_Token
     CHAT_ID=-100XXXXXXXXXX
     AUTH_TOKEN=您的_Twitter_Auth_Token_在此
     CSRF_TOKEN=您的_Twitter_CSRF_Token_在此
     TWITTER_BEARER_TOKEN=AAAAAAAAAAAAAAAAAAAAANRILgAAAAAAnNwIzUejRCOuH5E6I8xnZz4puTs%3D1Zv7ttfk8LF81IUq16cHjhLTvJu4FA33AGWWjCpTnA
     ```
     *(附带提示：如果您在国内需要走代理，可以取消 `.env` 中 `PROXY_URL` 的注释并填入您的本地代理地址)*

4. **配置要监控的推特博主**：
   - 打开 `config.json` 文件，按照 `{"用户数字ID": "@用户推特句柄"}` 的格式添加或修改：
     ```json
     {
         "2269758835": "@BingXOfficial",
         "2607585513": "@BitFlashNetwork"
     }
     ```

### 如何获取那必须的三个推特 Token？

要拿到脚本所需的 `AUTH_TOKEN` 和 `CSRF_TOKEN`，请按照以下“傻瓜式”步骤操作：

1. **在电脑浏览器打开 Twitter** 并登录您的账号。
2. **打开开发者工具**：
   - 键盘按下 `F12`，或者按 `Ctrl + Shift + I` (Windows)，`Cmd + Option + I` (Mac)。
3. **寻找包含身份证明的 Cookies**：
   - 在开发者面板顶部，点击 **Application (应用)** 选项卡。
   - 在左侧边栏找到 **Storage -> Cookies -> https://x.com**。
4. **抄写关键密码**：
   - **寻找 `auth_token`**：在列表里找到名为 `auth_token` 的那一行，复制它的值（一长串字符），填入 `.env` 里的 `AUTH_TOKEN`。
   - **寻找 `ct0`**：在列表里找到名为 `ct0` 的那一行，复制它的值，填入 `.env` 里的 `CSRF_TOKEN`。
   - *(注：`TWITTER_BEARER_TOKEN` 目前填默认模板里的固定值即可)*

全都填进 `.env` 后，您就大功告成了！

---

## 如何运行

在终端中执行以下命令启动脚本：

```bash
python main.py
```
*(或者使用虚拟环境： `./venv/bin/python main.py`)*

机器人将启动常驻轮询模式，死死盯住您配置的博主，一旦有新推文发布，几秒钟内您的 Telegram 就会收到振动提醒！

## 配置文件详解

### 环境变量 (`.env`)

- `BOT_TOKEN`: 您的 Telegram 机器人 Token。
- `CHAT_ID`: 接收消息的群组/频道 ID。
- `AUTH_TOKEN`: 代表您推特登录身份的核心凭证。
- `CSRF_TOKEN`: 推特用来防范跨站请求伪造的安全校验码。
- `TWITTER_BEARER_TOKEN`: 推特前端通用 API 接口调用凭证。
- `PROXY_URL`: （可选）如果访问不了 Telegram API，在这里配置您的 HTTP/SOCKS 代理。

### 核心配置文件 (`config.json`)

- 这个文件里的内容是一个映射表，格式为 `{"用户数字ID": "用户名"}`。脚本轮询时是依据“数字 ID”去拉取推文的，用户名仅为了最终发消息时展示好看。

---

## 技术支持与交流

如果在部署过程中遇到任何天坑，或者有功能上的奇思妙想，欢迎点亮 Star 并提交 Issue！
> **代码仓库**: [https://github.com/ChowKwanhwa/real-time-twitter-alert](https://github.com/ChowKwanhwa/real-time-twitter-alert)

---

<h2 align="center">防掉线玄学计数器 �</h2>
<a href="https://github.com/ChowKwanhwa/real-time-twitter-alert"><img alt="Cute Count" src="https://count.getloli.com/@real-time-twitter-alert?name=real-time-twitter-alert&theme=love-and-deepspace&padding=7&offset=0&align=top&scale=1&pixelated=1&darkmode=auto" /></a>
